CREATE TYPE BODY racional es mapa MEMBER FUNCTION rat_to_real RETURN REAL IS BEGIN RETURN numerador/denominador; END; MEMBER PROCEDURE normalizar es gcd número: = integer_operations.greatest_common_divisor (numerador, denominador); BEGIN numerador: = numerador/MCD; denominador: = denominador/MCD; END; MEMBER FUNCTION plus(x rational) RETURN racional es r racional: = rational_operations.make_rational (numerator*x.denominator + x.numerator*denominator, denominador * x.  denominador); BEGIN RETURN r; END; END; 